---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm
 
steps:
 - name: build
   image: alpinelinux/docker-cli
   commands:
     - docker build - < Dockerfile.arm

trigger:
  branch:
  - arm
